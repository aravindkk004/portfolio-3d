import React from "react";
import { Tilt } from "react-tilt";
import { motion } from "framer-motion";
import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className="bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]"
      >
        As an accomplished Full Stack Developer and App Developer proficient in
        leveraging the MERN stack for web development and React Native for
        mobile app development, I am driven by a fervent passion for innovation
        and relentless pursuit of growth. While boasting a robust portfolio of
        crafting seamless, user-centric applications, I am equally excited about
        delving into new territories, particularly Artificial Intelligence (AI).
        Though I am currently unfamiliar with AI technologies, I am eager and
        committed to expanding my expertise in this area. With a resolute
        determination to learn and adapt, I am excited about the prospect of
        integrating AI into my development projects, ultimately aiming to
        pioneer revolutionary advancements that redefine the technology
        landscape. Through unwavering dedication and a steadfast commitment to
        excellence, I am poised to embark on a journey of discovery in AI and
        lead the development of cutting-edge solutions that push the boundaries
        of innovation.
      </motion.p>

      <div className="mt-20 flex flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
